server:
  port: 8081

spring:
  application:
    name: auction-notification

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379

  cloud:
    aws:
      parameterstore:
        enabled: false

management:
  endpoints:
    web:
      exposure:
        include:
          - health
  endpoint:
    health:
      show-details: never

jwt:
  secret:
    key: ${JWT_SECRET}
  accessExpire: 1800000
  refreshExpire: 604800000